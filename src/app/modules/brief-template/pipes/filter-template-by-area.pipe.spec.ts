/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { FilterTemplateByAreaPipe } from './filter-template-by-area.pipe';

describe('Pipe: FilterTemplateByAreae', () => {
  it('create an instance', () => {
    let pipe = new FilterTemplateByAreaPipe();
    expect(pipe).toBeTruthy();
  });
});
