/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { ShowCategoriesListPipe } from './show-categories-list.pipe';

describe('Pipe: ShowCategoriesListe', () => {
  it('create an instance', () => {
    let pipe = new ShowCategoriesListPipe();
    expect(pipe).toBeTruthy();
  });
});
