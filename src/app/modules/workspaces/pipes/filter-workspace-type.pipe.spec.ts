/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { FilterWorkspaceTypePipe } from './filter-workspace-type.pipe';

describe('Pipe: FilterWorkspaceTypee', () => {
  it('create an instance', () => {
    let pipe = new FilterWorkspaceTypePipe();
    expect(pipe).toBeTruthy();
  });
});
