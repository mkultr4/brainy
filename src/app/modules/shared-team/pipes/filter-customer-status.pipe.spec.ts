/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { FilterCustomerStatusPipe } from './filter-customer-status.pipe';

describe('Pipe: FilterCustomerStatuse', () => {
  it('create an instance', () => {
    let pipe = new FilterCustomerStatusPipe();
    expect(pipe).toBeTruthy();
  });
});