/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { FilterTeamAccountStatusPipe } from './filter-team-account-status.pipe';

describe('Pipe: FilterTeamAccountStatuse', () => {
  it('create an instance', () => {
    let pipe = new FilterTeamAccountStatusPipe();
    expect(pipe).toBeTruthy();
  });
});