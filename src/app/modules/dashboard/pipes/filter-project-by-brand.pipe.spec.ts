/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { FilterProjectByBrandPipe } from './filter-project-by-brand.pipe';

describe('Pipe: FilterProjectByBrande', () => {
  it('create an instance', () => {
    let pipe = new FilterProjectByBrandPipe();
    expect(pipe).toBeTruthy();
  });
});
