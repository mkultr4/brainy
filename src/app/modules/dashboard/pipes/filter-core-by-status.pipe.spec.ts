/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { FilterCoreByStatusPipe } from './filter-core-by-status.pipe';

describe('Pipe: FilterCoreByStatuse', () => {
  it('create an instance', () => {
    let pipe = new FilterCoreByStatusPipe();
    expect(pipe).toBeTruthy();
  });
});
