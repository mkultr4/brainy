import { SidenavButtonDirective } from './sidenav-button.directive';

describe('SidenavButtonDirective', () => {
  it('should create an instance', () => {
    const directive = new SidenavButtonDirective();
    expect(directive).toBeTruthy();
  });
});
