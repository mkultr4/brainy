/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { FilterCommentMessagePrimaryPipe } from './filter-comment-message-primary.pipe';

describe('Pipe: FilterCommentMessagePrimarye', () => {
  it('create an instance', () => {
    let pipe = new FilterCommentMessagePrimaryPipe();
    expect(pipe).toBeTruthy();
  });
});
