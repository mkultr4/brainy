/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { FilterCoreByBrandPipe } from './filter-core-by-brand.pipe';

describe('Pipe: FilterCoreByBrande', () => {
  it('create an instance', () => {
    let pipe = new FilterCoreByBrandPipe();
    expect(pipe).toBeTruthy();
  });
});
