/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { FilterCoreByProjectPipe } from './filter-core-by-project.pipe';

describe('Pipe: FilterCoreByProjecte', () => {
  it('create an instance', () => {
    let pipe = new FilterCoreByProjectPipe();
    expect(pipe).toBeTruthy();
  });
});
