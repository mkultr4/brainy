/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { FilterInvitationPendingPipe } from './filter-invitation-pending.pipe';

describe('Pipe: FilterInvitationPendinge', () => {
  it('create an instance', () => {
    let pipe = new FilterInvitationPendingPipe();
    expect(pipe).toBeTruthy();
  });
});
