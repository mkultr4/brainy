import { DropdownWrapperDirective } from './dropdown-wrapper.directive';

describe('DropdownWrapperDirective', () => {
  it('should create an instance', () => {
    const directive = new DropdownWrapperDirective();
    expect(directive).toBeTruthy();
  });
});
