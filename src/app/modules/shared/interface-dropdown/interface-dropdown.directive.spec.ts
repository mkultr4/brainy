/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { InterfaceDropdownDirective } from './interface-dropdown.directive';

describe('Directive: InterfaceDropdown', () => {
  it('should create an instance', () => {
    const directive = new InterfaceDropdownDirective();
    expect(directive).toBeTruthy();
  });
});
